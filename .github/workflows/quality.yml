name: Quality

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DO_NOT_TRACK: "1"
      HF_HUB_DISABLE_TELEMETRY: "1"
      HF_HUB_OFFLINE: "1"
      HF_HOME: /tmp/jgrad-hf-home
      TRANSFORMERS_OFFLINE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project and development checks
        run: python -m pip install -e ".[dev,service,generation]"
      - name: Run offline tests
        run: python -m pytest -m "not model_integration"
      - name: Lint and format
        run: |
          ruff check . --no-cache
          ruff format --check . --no-cache
      - name: Compile sources and tests
        run: python -m compileall -q src tests
      - name: Check patch whitespace
        run: git diff --check
      - name: Check frozen semantic retrieval baseline
        run: |
          jgrad-check-retrieval-gate \
            --report tests/fixtures/semantic_retrieval_baseline_v1.json \
            --policy config/semantic_retrieval_gate_v1.json \
            --manifest config/semantic_retrieval_gate_manifest_v1.json \
            --repository-root .
      - name: Check grounded RAG release baseline
        run: |
          jgrad-check-grounded-rag-gate \
            --suite tests/fixtures/grounded_rag_evaluation_suite_v1.json \
            --observations tests/fixtures/grounded_rag_observations_v1.json \
            --retrieval-benchmark tests/fixtures/grounded_rag_retrieval_queries_v1.json \
            --retrieval-report tests/fixtures/grounded_rag_retrieval_report_v1.json \
            --report tests/fixtures/grounded_rag_evaluation_report_v1.json \
            --policy config/grounded_rag_release_gate_v1.json \
            --repository-root .
      - name: Confirm no Hugging Face cache was created
        run: test ! -e "$HF_HOME" || test -z "$(find "$HF_HOME" -mindepth 1 -print -quit)"
